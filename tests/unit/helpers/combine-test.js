import { combine } from 'aurora/helpers/combine';
import { module, test } from 'qunit';

module('Unit | Helper | combine');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = combine([42]);
  assert.ok(result);
});
