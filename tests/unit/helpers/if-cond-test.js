import { ifCond } from 'aurora/helpers/if-cond';
import { module, test } from 'qunit';

module('Unit | Helper | if cond');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifCond([42]);
  assert.ok(result);
});
